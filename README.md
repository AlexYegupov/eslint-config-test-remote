# Package to test eslint-config purely

##How to test & upgrade

```
yarn upgrade @wttsolutions/eslint-config
yarn --silent run eslint-config-version
yarn test
git commit -am"updated to version `yarn --silent run eslint-config-version`"
```
